services:
  tradebuddy:
    build:
      context: .
      dockerfile: Dockerfile.optimized
    ports:
      - "5000:80"
    env_file:
      - .env
    volumes:
      - playwright_cache:/app/ms-playwright
      - ./logs:/app/logs

volumes:
  playwright_cache:
