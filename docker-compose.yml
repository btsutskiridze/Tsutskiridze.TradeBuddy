services:
  tradebuddy:
    build:
      context: .
      dockerfile: Dockerfile.test
    ports:
      - "80:80"
    env_file:
      - .env
    volumes:
      - playwright_cache:/app/ms-playwright
      - ./logs:/app/logs

volumes:
  playwright_cache:
