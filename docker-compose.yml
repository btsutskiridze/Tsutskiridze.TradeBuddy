services:
  tradebuddy:
    build:
      context: ./Tsutskiridze.TradeBuddy.API
      dockerfile: Dockerfile.optimized
    ports:
      - "80:80"
    env_file:
      - .env
    volumes:
      - playwright_cache:/app/ms-playwright
      - ./logs:/app/logs

volumes:
  playwright_cache:
